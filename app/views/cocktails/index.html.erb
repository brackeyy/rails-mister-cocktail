




<h1>Here are all the cocktails 👀</h1>

<div class="container">
  <% @cocktails.each do |c| %>
    <p>---------------------------------</p>
  <p><strong>Cocktail:</strong> <%= c.name%> <%= link_to "Edit", c, method: :get, class: 'btn-sm btn-primary'%> </p>
    <p><strong>Recipe:</strong></p>
    <% c.doses.each do |d| %>
      <p> -> <%= d.ingredient.name %> ~ <%= d.description %> /p>

    <% end %>
  <% end %>
</div>
